@model NewServiсeViewModel
@{ViewData["Title"] = "Добавить новую услугу";
    Layout = "_LoginLayout";
}
<form asp-action="AddServiсe" method="post">
    <div class="card-body">
        <!--div asp-validation-summary="All" class="text-danger"></!div-->


        <div class="form-group bmd-form-group">
            <label asp-for="Serviсe" class="bmd-label-floating"></label>
            <input asp-for="Serviсe" class="form-control"
                   placeholder="Название новой услуги">
            <span asp-validation-for="Serviсe" class="text-danger"></span>
        </div>


    </div>

    <div class="card-footer justify-content-center">
        <button type="submit" class="btn btn-primary btn-fill btn-lg"> Сохранить изменения
        </button>
    </div>
</form>

@section Scripts {
    @await Html.PartialAsync("_ValidationServise")
}